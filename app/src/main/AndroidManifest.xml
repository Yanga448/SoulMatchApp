<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- 🌐 Internet permission (required for Firebase, Retrofit, etc.) -->
    <uses-permission android:name="android.permission.INTERNET" /> <!-- 📱 Optional: Wake up screen when receiving notification -->
    <uses-permission android:name="android.permission.WAKE_LOCK" /> <!-- 🔔 For posting notifications (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />


    <application
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SoulMatchApp"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <activity
            android:name=".NotificationActivity"
            android:exported="false" />
        <!-- 💾 Offline (Room) -->
        <activity
            android:name=".SavedProfilesActivity"
            android:exported="false" /> <!-- ⚙️ Settings -->
        <activity
            android:name=".SettingsActivity"
            android:exported="false" /> <!-- 🏠 Dashboard -->
        <activity
            android:name=".DashboardActivity"
            android:exported="false" /> <!-- 💌 Chat -->
        <activity
            android:name=".ChatActivity"
            android:exported="false" /> <!-- 👤 Profile details -->
        <activity
            android:name=".ProfileDetailsActivity"
            android:exported="false" /> <!-- 🚀 Splash (Launch screen) -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.SoulMatchApp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- 🔐 Login -->
        <activity
            android:name=".LoginActivity"
            android:exported="false"
            android:theme="@style/Theme.SoulMatchApp" /> <!-- 📝 Signup -->
        <activity
            android:name=".SignupActivity"
            android:exported="false"
            android:theme="@style/Theme.SoulMatchApp" /> <!-- ❤️ Main Activity (after login) -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:theme="@style/Theme.SoulMatchApp" /> <!-- 🔔 Firebase Messaging Service -->
        <service
            android:name=".MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>